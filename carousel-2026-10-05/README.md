# Stay or Leave? Seven Moves carousel, 2026-10-05

## The finding that matters

**The uploaded PDF is not the old version.** Two of the four requested changes
are already in it, verified by searching the text of all ten pages:

| Requested | State |
| --- | --- |
| Slide 5 wording | **Already applied.** It reads "the people who make hiring and promotion decisions cannot yet see what you are good at." The string "people with the power" appears nowhere in the file. |
| Slide 9 rewrite with Who to ask | **Already applied.** Titled "Seek an outside perspective", with the WHO TO ASK block: a former manager, a mentor, a peer in the field you are considering, or a career advisor. |
| START HERE Featured cover | **Built here**, as a draft. |
| Upload-ready zip | **Built here.** |

Move 5 also already reads "Repair the conditions", matching deck v2.0.10 and
workbook v2.0.3. All seven names on the carousel now match the deck and the
workbook exactly.

## The carousel was not edited

Its type is set as Type3 outlines, so the PDF is not text-editable: changing a
word means rebuilding it in the tool that made it. Nothing needed changing, so
nothing was attempted.

`sha256 f5419f499064074f14025d74207f3c933e782dd6c2202a2a46d153acc80b561b`,
10 pages, 810 x 1013.04pt, which is 4:5.

## What is in the zip

14 files.

- `2026-10-05_Stay_or_Leave_Seven_Moves.pdf` — the carousel, unchanged. Upload
  this one file as a LinkedIn document post.
- `slide_01_1080x1350.png` through `slide_10_1080x1350.png` — the same pages at
  LinkedIn's native document size, for an image carousel or a scheduler that
  will not take a PDF. Exported by scale, no crop and no letterbox, and the
  build asserts each export is exactly 1080 x 1350.
- `featured_START_HERE_1600x900.png` and `..._800x450.png` — the Featured cover.
- `README.txt`.

## The Featured cover is a draft, and here is what I assumed

Its **size** follows the repository's existing Featured assets in
`linkedin-featured-assets/`, which are 1600x900 and 800x450. That is the house
spec, not a guess.

Its **copy** is taken entirely from the carousel's own cover and closing page:
the title, the two lines beneath it and the byline. The only new words are the
eyebrow START HERE, which is what the cover is for.

Its **colors** are sampled from the carousel itself: navy `112345`, cream
`F5F1E8`, gold `C9A84C`, body `182238`, muted gold `7F6A30`.

Its **type** is DM Sans and Montserrat, the brand faces. The carousel's own font
is embedded as outlines and cannot be reused, so this is the closest available
match rather than the identical face.

If the cover should say something else, or sit at a different size, say so and
it is a two-minute change.
